import * as React from 'react';
import { View, Text } from 'react-native';

const Splash = () => {
    return(
        <View style={{flex: 1}}>
            <Text>Splash</Text>
        </View>
    )
}

export default Splash;